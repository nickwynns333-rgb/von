# OpenVoice and Meta Control Center UI Verification

**Verified:** 2026-08-26

The authenticated VonWork interface renders the new Social AI Control Center with channel command cards, aggregate monitoring statistics, provider-safe setup guidance, and no externally delivering test action. The left navigation exposes the wider AI agents and Call Center surface alongside the new control center.

The Voice Studio renders the dedicated **OpenVoice for chatbots and video agents** panel, displays the configured/not-configured gateway state without exposing credentials, explains the OpenRouter text-reasoning separation, and keeps custom voice creation behind a signed rights and disclosure attestation.

These checks validate the interface and safe fallback state only. Live OpenVoice speech requires the configured GPU-hosted service, while Messenger and Instagram delivery requires a configured Meta app, account identifiers, tokens, and verified webhook subscription.
